// import { Button } from "@/components/ui/button";

// interface Props {
// tables: { id: string; name: string }[];
// activeTableId: string | null;
// onSelect: (id: string) => void;
// }

// export default function MobileTableSelector({
// tables,
// activeTableId,
// onSelect,
// }: Props) {
// return ( <div className="md:hidden flex gap-2 overflow-x-auto px-2 py-2 border-b bg-background">

// ```
//   {tables.map((t) => (
//     <Button
//       key={t.id}
//       size="sm"
//       variant={activeTableId === t.id ? "default" : "outline"}
//       className="whitespace-nowrap h-8"
//       onClick={() => onSelect(t.id)}
//     >
//       {t.name}
//     </Button>
//   ))}

// </div>
// ```

// );
// }

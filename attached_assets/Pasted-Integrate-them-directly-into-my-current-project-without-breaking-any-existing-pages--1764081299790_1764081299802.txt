Integrate them directly into my current project without breaking any existing pages:

---------------------------------------------------
🔐 FIREBASE LOGIN + AUTH SYSTEM REQUIREMENTS
---------------------------------------------------

1️⃣ Connect to my Firebase project using my `firebaseConfig` (I’ll paste it when you ask).

2️⃣ Create a secure login page with:
- email input
- password input
- password show/hide toggle
- loading state
- error handling
- professional UI design

3️⃣ Login logic must use THIS security flow (merge with my project):
- fetch Firestore user document
- check `role` (admin/client)
- check `active`
- check `expiry`
- check `deviceLimit`
- store deviceId using localStorage
- if device limit reached → show error and logout
- if login successful → redirect based on role:
    - admin → /admin/dashboard
    - client → /client/dashboard

4️⃣ Prevent login bypass:
- Add ProtectedRoute component
- Add AdminRoute & ClientRoute
- If user not logged in → redirect to /login
- If wrong role tries to access wrong page → block + redirect

5️⃣ Keep user logged in until manual logout:
- Use Firebase's `onAuthStateChanged`
- When user refreshes page, auto-restore login
- Do not re-check device limit again on refresh

6️⃣ Add secure logout:
- remove deviceId from Firestore devices array
- sign out user
- clear role + user state
- redirect to /login

7️⃣ Update router so that:
- /login is public
- all other pages are protected
- admin pages only for admin
- client pages only for client

8️⃣ Make sure the login page cannot be accessed once logged in (auto redirect to dashboard).

9️⃣ Create helper file:
- getDeviceId() → store unique ID in localStorage

10️⃣ DO NOT modify my existing POS features.  
Just integrate login + role system cleanly into my structure.

-----------------------------------------------
⚡ IMPORTANT  
Use my existing code style, folder structure and components.  
Modify only where necessary.  